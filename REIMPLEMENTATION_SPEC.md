# 排列五分析台复现规格

## 1. 目标

这份文档不是对现有代码的逐行解释，而是给另一个 AI 的“可执行实现规格”。

目标是让另一个 AI 在不知道当前源码细节的前提下，仍然能完整复现本项目的功能，并且避免把当前代码里已经发现的缺陷原样照抄过去。

复现后的项目需要满足下面两点：

1. 用户可见功能与当前项目保持一致。
2. 当前项目里已知的明显问题要修掉，保证“功能正常”。

## 2. 技术栈与目录结构

### 2.1 技术栈

- 后端：Python 3.9+，Flask
- 数据库：SQLite
- 前端：原生 HTML + CSS + Vanilla JS
- 依赖极少，不使用前端框架

### 2.2 目录结构

应保持为下面这类结构：

```text
project/
  app.py
  requirements.txt
  lottery.db              # 可由程序自动初始化，也可以首次启动时生成
  templates/
    index.html
  static/
    styles.css
    script.js
    favicon.png
```

### 2.3 requirements

```txt
Flask==3.0.3
Werkzeug==3.0.3
```

## 3. 产品定位与页面功能

这是一个“排列五分析台”，首页只有一个主页面，用户核心操作是：

1. 从本地 SQLite 读取最近 N 期历史开奖。
2. 点击“更新开奖”从远程 XML 拉最新数据并写入本地库。
3. 对最近 N 期数据做：
   - 遗漏统计
   - 大底生成
   - 大底容错分析
   - 大底转换分析
4. 打开全屏矩阵，对原始 5 位开奖号码做组合命中、追踪和搜索。

## 4. 页面结构

### 4.1 顶部区域

页面主标题：

- `cwh868 彩票专用 · 排列五分析台`

副标题：

- `数据加载、遗漏分析、矩阵追踪一体化工作台`

### 4.2 左上卡片

左上卡片包含：

1. 最新开奖横幅
   - 默认隐藏
   - 有数据时显示
   - 显示最新开奖日期，格式为 `MM-DD`
   - 显示最新一期开奖结果 5 个红色圆球数字
2. 期数下拉框
   - 选项：`100, 200, 300, 400, 500, 600, 700, 800, 900, 1000`
   - 默认值：`300`
   - 变更后立即重新加载历史数据并重新分析
3. `更新开奖` 按钮
   - 点击后调用后端更新接口
   - 按钮在请求中禁用并显示 `更新中...`
   - 完成后恢复文字

### 4.3 右上区域

显示“当前分析维度”：

- 默认 `1 期`
- 按钮共 20 个：
  - 第一个按钮文字是 `1 期`
  - 第 2 到第 20 个按钮文字分别是 `2 期和` 到 `20 期和`

还有一个按钮：

- `展示矩阵`

### 4.4 下方主内容

两列布局：

1. 左侧：数据预览
   - 默认提示：`加载数据后在此处预览`
   - 加载历史数据后显示每一行开奖号
   - 重新分析后：
     - 如果当前维度是 `1 期`，显示原始开奖号
     - 如果当前维度是 `k 期和`，显示逐位和模 10 的转换结果
2. 右侧：统计结果
   - 默认提示：`点击「更新开奖」查看结果`
   - 结果区域为 tab 结构，共 4 个 tab：
     - `遗漏统计`
     - `大底生成`
     - `大底容错分析`
     - `大底转换`

## 5. 数据库设计

### 5.1 数据库路径

不要使用相对路径 `lottery.db` 直接依赖当前工作目录。

应使用类似下面的方式固定到项目目录：

```python
from pathlib import Path
DB_PATH = Path(__file__).with_name("lottery.db")
```

### 5.2 表结构

#### 表 1：`lottery_history`

```sql
CREATE TABLE IF NOT EXISTS lottery_history (
    expect TEXT PRIMARY KEY,
    opencode TEXT NOT NULL,
    opentime TEXT NOT NULL
);
```

字段语义：

- `expect`: 期号，字符串主键
- `opencode`: 5 位开奖号码，纯数字字符串，例如 `64915`
- `opentime`: 开奖时间字符串，例如 `2026-03-07 21:25:00`

#### 表 2：`dadi_base_sets`

```sql
CREATE TABLE IF NOT EXISTS dadi_base_sets (
    slot INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    numbers_text TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

字段语义：

- `slot`: 固定 1 到 4
- `name`: 大底名称
- `numbers_text`: 多行文本，每行一个三位号码
- `updated_at`: 更新时间

## 6. 数据初始化规则

### 6.1 启动时初始化

应用启动时必须做两件事：

1. 建表
2. 确保 `dadi_base_sets` 存在 `slot=1..4`

### 6.2 历史开奖初始化

如果 `lottery_history` 为空，则尝试从下面的 XML 地址抓取：

```text
https://kaijiang.500.com/static/info/kaijiang/xml/plw/list.xml
```

抓取规则：

- 使用 `urllib.request`
- 自定义 `User-Agent: Mozilla/5.0`
- 超时 10 秒
- 解析 XML 中每个 `<row>`
- 读取字段：
  - `expect`
  - `opencode`
  - `opentime`
- `opencode` 原格式是逗号分隔，需去掉逗号
- 只接收长度正好为 5 的号码
- 重复期号使用 `INSERT OR IGNORE`

重要要求：

- 即使网络失败，应用也必须能继续启动
- 不能因为初始化抓取失败导致首页 500

### 6.3 大底库初始化

必须提供 4 个大底槽位：

1. `slot=1`，名称 `798大底`
2. `slot=2`，名称 `无全大大底`
3. `slot=3`，名称 `无全质大底`
4. `slot=4`，名称 `组六大底`

#### slot=2 生成规则

包含 `000` 到 `999` 中所有三位数，但排除“全大”号码。

“全大”定义为 3 个数字都属于 `5,6,7,8,9`。

所以 slot=2 的数量必须是：

- `1000 - 5^3 = 875`

#### slot=3 生成规则

包含 `000` 到 `999` 中所有三位数，但排除“全质”号码。

这里“质”使用当前项目的定义：

- `1, 2, 3, 5, 7`

也就是说，排除三位都在集合 `{1,2,3,5,7}` 内的号码。

所以 slot=3 的数量也必须是：

- `1000 - 5^3 = 875`

#### slot=4 生成规则

只包含“组六”号码，即三位数字两两不同。

例如：

- `012` 包含
- `121` 不包含
- `999` 不包含

数量必须是：

- `10 * 9 * 8 = 720`

#### slot=1 生成规则

当前项目中的 `slot=1` 不是一个容易由单条规则推导出的简单集合。

为了确保和当前项目完全一致，应直接使用精确种子数据，而不要自行猜规则。

要求：

- 名称固定为 `798大底`
- 总数固定为 `798`
- 精确内容见本文附录 A

## 7. 后端 API 设计

## 7.1 `GET /`

返回首页模板 `index.html`。

## 7.2 `GET /api/get_history`

### 查询参数

- `limit`: 整数，默认 `300`

### 行为

1. 从 `lottery_history` 按 `expect DESC` 取最新 `limit` 条
2. 先拿第一条的 `opentime` 作为 `latest_time`
3. 再把开奖号码列表反转为“旧 -> 新”的顺序返回

### 返回格式

```json
{
  "success": true,
  "data": ["64915", "74492", "65564", "53752", "11921"],
  "latest_time": "2026-03-07 21:25:00"
}
```

### 约束

- `limit` 必须钳制在 `100..1000`
- 不能允许 `-1` 这种值把整库都读出来
- 如果没有数据，返回 404 和错误信息

## 7.3 `POST /api/update_history`

### 行为

与初始化抓取逻辑一致，但用于手动更新：

1. 请求远程 XML
2. 解析为 5 位号码
3. 对每条数据按 `expect` 去重
4. 插入新增数据

### 返回格式

```json
{
  "success": true,
  "added_count": 12,
  "message": "成功更新了 12 条新数据"
}
```

### 实现要求

- 不要用“先 SELECT 再 INSERT”的低效方式
- 直接使用 `INSERT OR IGNORE`，再统计插入数即可

## 7.4 `GET /api/dadi_bases`

### 作用

返回 4 个大底槽位的概要信息。

### 返回格式

```json
{
  "success": true,
  "bases": [
    { "slot": 1, "name": "798大底", "sourceCount": 798 },
    { "slot": 2, "name": "无全大大底", "sourceCount": 875 },
    { "slot": 3, "name": "无全质大底", "sourceCount": 875 },
    { "slot": 4, "name": "组六大底", "sourceCount": 720 }
  ]
}
```

注意：

- 前端当前没有显式的大底编辑 UI
- 但这个接口和写入接口必须存在，因为大底转换功能依赖这些数据

## 7.5 `POST /api/dadi_bases/<slot>`

### 作用

更新某个大底槽位。

### `slot`

- 只允许 `1..4`

### 支持两种提交方式

#### 方式 A：JSON

```json
{
  "name": "自定义名称",
  "numbers": ["123", "456", "789"]
}
```

或者：

```json
{
  "name": "自定义名称",
  "numbers": "123 456 789"
}
```

#### 方式 B：表单 / 文件上传

- `name`: 可选
- `numbers`: 可选，文本
- `file`: 可选，txt 文件

### 号码解析规则

应实现 `normalize_dadi_numbers(raw_input)`：

1. 若输入是数组，则逐项拆分
2. 若输入是字符串，则整体拆分
3. 分隔符支持：
   - 空白
   - 英文逗号 `,`
   - 中文逗号 `，`
   - 英文分号 `;`
   - 中文分号 `；`
4. 只接受严格三位数字：`^\d{3}$`
5. 去重并保留原始出现顺序
6. 所有非法 token 要收集出来

### 返回

成功：

```json
{
  "success": true,
  "slot": 1,
  "name": "自定义名称",
  "sourceCount": 123
}
```

失败：

- 非法 token 返回 400
- 错误信息需包含前几个非法值预览

### 安全要求

- `name` 不允许在前端被直接插入 `innerHTML`
- 后端可以原样存储，但前端渲染必须转义

## 7.6 `POST /api/analyze`

### 请求格式

必须是 `multipart/form-data`，前端通过 `Blob + FormData` 提交：

- `file`: 文本文件
- `period_sum`: 1 到 20 的整数

### 输入文件格式

- 每行一个开奖号码
- 每行必须是 5 位纯数字
- 至少 2 行

注意：

- 为了让功能正常，必须拒绝非 5 位输入
- 当前代码对非 5 位输入有隐藏 500 风险，复现时要修掉

### 返回格式

```json
{
  "totalFiles": 1,
  "totalLines": 300,
  "parsedData": ["....."],
  "gapAnalysis": [...],
  "dadiAnalysis": {...},
  "dadiFaultTolerance": {...},
  "dadiTransform": {...},
  "dangerPeriods": [3, 5, 11],
  "offsets": [0, 0, 0, 0, 0],
  "periodSum": 1
}
```

其中字段含义详见后面的算法章节。

## 8. 核心算法

## 8.1 原始数据预处理

设输入行列表为 `lines`。

要求：

1. 去掉空白行
2. 所有行都必须是 5 位纯数字
3. 至少要有 2 行

构造：

```python
raw_parsed = [[int(ch) for ch in line] for line in lines]
```

记：

- `n_lines = len(lines)`
- `L = 5`

## 8.2 当前分析维度：`period_sum`

### 规则

#### `period_sum == 1`

- `parsed = raw_parsed`

#### `period_sum > 1`

对每个连续窗口做逐位和模 10：

```python
for i in range(n_lines - period_sum + 1):
    window = raw_parsed[i:i + period_sum]
    summed_row[pos] = sum(row[pos] for row in window) % 10
```

输出的 `parsed` 长度为：

- `n_lines - period_sum + 1`

### 特别说明

- 页面主分析区的预览和统计都使用当前 `parsed`
- 但矩阵功能始终使用原始 `currentSourceLines`，也就是原始 5 位开奖号，不跟随 `period_sum` 变化

这个行为要保留，因为当前项目就是这样。

## 8.3 遗漏统计

基于当前 `parsed` 做 5 个位置的遗漏分析。

### 步骤

1. 对每个位置 `pos=0..4` 和数字 `0..9`，记录最后一次出现的行号
2. 若某数字从未出现，则 gap = `n`
3. 否则：

```python
gap = n - last_appeared_row
```

这里当前项目使用的是“1-based 行号”。

### 输出结构

每个位置返回：

```json
{
  "position": 1,
  "maxGap": 8,
  "candidates": [2, 3, 8, 9]
}
```

### UI 规则

- 总共 5 张卡片
- 如果某位置 `maxGap > 40`，该卡片高亮为危险状态
- 同时顶部对应的期数按钮，如果该期数被判定为危险，也要加红边危险样式

## 8.4 大底生成 `compute_dadi(k_period)`

这个算法只产出三位号码，与 5 位原始数据的“前 3 位逻辑”相关。

### 前置条件

1. `n_lines >= k_period + 1`
2. 先对 `raw_parsed` 做一个 `k_period` 的逐位和模 10 序列 `parsed_k`
3. `len(parsed_k) >= 3`

### 构造 `parsed_k`

```python
for i in range(n_lines - k_period + 1):
    window = raw_parsed[i:i + k_period]
    summed_row[pos] = sum(row[pos] for row in window) % 10
```

### 取最近三行前三位，形成组 1 / 2 / 3

定义 `extract_top3_unique(digits_row)`：

- 只看该行前 3 位
- 从左到右去重
- 保留首次出现顺序

例如：

- `[1, 1, 2, 9, 9]` -> `[1, 2]`
- `[3, 4, 5, 0, 1]` -> `[3, 4, 5]`

然后：

- `group1 = parsed_k[-1]` 的前三位去重结果
- `group2 = parsed_k[-2]` 的前三位去重结果
- `group3 = parsed_k[-3]` 的前三位去重结果

### 组 4

```python
all_app = set(group1 + group2 + group3)
group4 = [d for d in range(10) if d not in all_app]
```

### 组 5 的构造

`group5` 是无序双码集合，规则必须完全一致：

1. `group1` 中每个数，和 `group2 + group3 + group4` 中每个数配对
2. `group2` 中每个数，和 `group3 + group4` 中每个数配对
3. `group3` 中每个数，和 `group4` 中每个数配对
4. 每个 pair 需要排序后再入集合，保证 `(2,5)` 和 `(5,2)` 视为同一个

### 遍历 000 到 999 形成大底

对每个三位数 `s = f"{num:03d}"`：

1. 如果某个 pair 两位相同，例如 `(5,5)`，则要求该三位数中数字 `5` 至少出现 2 次
2. 如果某个 pair 两位不同，例如 `(2,7)`，则要求该三位数同时包含 2 和 7
3. 只要命中任意一个 pair，该号码有效

### 多期和偏移修正

如果 `k_period > 1`，还需要做偏移修正：

1. 取原始数据最后 `k_period - 1` 行
2. 对每一位求和模 10，得到 `offset_all`
3. 只对三位候选号码的前三位做：

```python
final_digit = (digit - offset_all[pos]) % 10
```

4. 输出修正后的三位字符串

### 输出

```json
{
  "group1": [1, 4, 7],
  "group2": [0, 3, 8],
  "group3": [2, 5],
  "group4": [6, 9],
  "group5": [[1, 0], [1, 2], ...],
  "dadi": ["000", "001", "..."],
  "offsetApplied": true,
  "offsets": [1, 8, 3, 0, 7]
}
```

注意：

- `dadi` 最终需要去重并按字符串升序排序
- `offsets` 在返回里保留 5 位长度，便于 gap 区域也能显示位移箭头

## 8.5 大底容错统计

### 目的

统计 `k=1..20` 各期和下的大底结果对每个三位号码的命中次数。

### 步骤

对每个 `k in 1..20`：

1. 调用 `compute_dadi(k)`
2. 如果返回有效结果，则：
   - `total_sets += 1`
   - 对 `res_k["dadi"]` 内每个号码计数 `+1`

### 输出

```json
{
  "total_sets": 20,
  "counts": {
    "000": 3,
    "001": 7
  }
}
```

### 前端容错筛选逻辑

用户设置：

- 容错下限 `minErr`
- 容错上限 `maxErr`

换算逻辑：

```python
minCount = max(0, total_sets - maxErr)
maxCount = total_sets - minErr
```

保留：

- `minCount <= counts[code] <= maxCount`

初始默认值：

- `minErr = 0`
- `maxErr = 2`

## 8.6 全局遗漏预警 `dangerPeriods`

### 目的

对每个 `k in 1..20`，检查前 3 位是否存在某个数字的遗漏大于 40。

### 步骤

1. 若 `k > 1`，先生成该 `k` 的逐位和模 10 序列 `pk`
2. 若 `k == 1`，则 `pk = raw_parsed`
3. 只检查前 3 个位置
4. 对每个位置 `pos=0..2` 和数字 `0..9`：
   - 从后往前找最后一次出现
   - 没出现则 gap = `nk`
   - 否则 gap = `nk - last_index`
5. 只要某个 gap `> 40`，该 `k` 就进入 `dangerPeriods`

### 输出

```json
[3, 5, 11]
```

### 前端规则

- 顶部 20 个期数按钮中，命中的按钮加 `danger-btn`

## 8.7 大底转换 `compute_dadi_transform(raw_parsed, digit_len, base_sets)`

### 目的

把每个大底库做：

- 1 组原始大底
- 19 组基于最近 `1..19` 期的偏移转换

注意：

- 这个算法也只使用前三位

### 输入

- `raw_parsed`: 原始 5 位开奖号列表
- `digit_len`: 5
- `base_sets`: 从 `dadi_base_sets` 读取出的 4 组大底

### 对每个 base 的处理

设：

- `numbers` 为该 base 的三位号码数组

#### 原始组

1. 把 `numbers` 作为 `period=0`
2. `offset = [0, 0, 0]`
3. 记入 `counts`
4. `totalSets += 1`

#### 偏移组

对 `k=1..19`：

1. 若历史条数 `< k`，停止
2. 取原始数据最后 `k` 行
3. 计算前 3 位 offset：

```python
offset[pos] = sum(row[pos] for row in window) % 10
```

4. 对 base 内每个三位号 `num`：

```python
conv = ''.join(str((int(num[pos]) - offset[pos]) % 10) for pos in range(3))
```

5. 同一组内先去重，再累加到总 `counts`
6. `totalSets += 1`

### 输出格式

```json
{
  "bases": [
    {
      "slot": 1,
      "name": "798大底",
      "sourceCount": 798,
      "totalSets": 20,
      "counts": {
        "000": 4,
        "001": 8
      },
      "offsets": [
        { "period": 0, "offset": [0, 0, 0], "is_original": true },
        { "period": 1, "offset": [2, 5, 7], "is_original": false }
      ]
    }
  ]
}
```

### 前端过滤逻辑

和“大底容错分析”同一套：

```python
minCount = max(0, totalSets - maxErr)
maxCount = totalSets - minErr
```

然后从 `000..999` 中筛选命中次数在区间内的号码。

### UI 规则

1. 顶部显示 4 个大底切换按钮
2. 每个 slot 单独记住自己的容错上下限
3. 初始默认仍然是 `0..2`
4. 元信息区域显示：
   - 原始大底注数
   - 转换组数
   - 容错区间
5. 支持复制结果和下载 TXT

## 9. 前端交互流程

## 9.1 启动流程

页面 `DOMContentLoaded` 后：

1. 读取 `localStorage['lottery_load_limit']`
2. 如果没有，则默认 `300`
3. 同步主页面期数下拉和矩阵中的期数下拉
4. 动态生成 20 个期和按钮
5. 默认选中 `1 期`
6. 立即调用 `loadHistoryData()`

## 9.2 `loadHistoryData(limitOverride = null)`

### 流程

1. 归一化 limit 到 `100..1000` 且以 100 为步长
2. 调用 `/api/get_history?limit=...`
3. 成功后：
   - `netLines = data.data`
   - `currentSourceLines = [...netLines]`
   - `latestOpenDate = latest_time` 提取为 `MM-DD`
   - `latestOpenNumbers = 最后一行开奖号码`
   - 渲染左侧预览
   - 调用 `doAnalyze(netLines)`
   - 如果矩阵已打开，刷新矩阵底层数据

### limit 归一化规则

```js
const parsed = parseInt(String(value || ''), 10);
const stepped = Math.round(parsed / 100) * 100;
return Math.max(100, Math.min(1000, stepped));
```

## 9.3 顶部期和按钮行为

点击任意 `k`：

1. 更新按钮 active 状态
2. 更新标题中的当前维度文字
3. 若已有 `netLines`，直接 `doAnalyze(netLines)`

## 9.4 `doAnalyze(linesOverride = null)`

### 流程

1. 取当前 raw 行列表
2. 拼成带换行的纯文本
3. 用 `Blob` 构造一个伪文件 `net_data.txt`
4. 用 `FormData` 发送到 `/api/analyze`
5. 成功后：
   - 用 `parsedData` 重绘左侧预览
   - 用完整结果重绘右侧 tab 区
   - 用 `dangerPeriods` 标记顶部按钮危险状态

## 9.5 复制 / 下载

### 复制

优先使用：

```js
navigator.clipboard.writeText(text)
```

失败时回退到 `document.execCommand('copy')`。

### 下载

使用 `Blob` + `<a download>` 下载 txt 文件。

文件名格式：

```text
<前缀>_YYYYMMDD_HHMMSS.txt
```

前缀示例：

- `大底生成结果`
- `大底容错结果`
- `大底转换_<大底名称>`

## 10. Tab 区 UI 细节

## 10.1 `遗漏统计`

### 展示形式

- 5 张卡片
- 每张卡片显示：
  - 大号背景位次数字
  - `遗漏 X 期`
  - 候选数字徽标

### 多期和时的箭头显示

当 `periodSum > 1` 且存在 `offsets` 时，候选数字后要显示：

- 原数字
- 箭头 `→`
- 修正后的真实数字

修正规则：

```js
realDigit = (d - offset + 10) % 10
```

## 10.2 `大底生成`

分 3 个折叠步骤：

1. 规则溯源分组
2. 组合 5 双码集合
3. 输出大底结果

展示项：

- `group1`
- `group2`
- `group3`
- `group4`
- `group5`
- 最终大底号码网格

若 `offsetApplied` 为真，显示红色偏移提示条。

## 10.3 `大底容错分析`

展示：

1. 容错条件设置区
2. 结果头部
   - 当前注数
   - 复制按钮
   - 下载按钮
3. 结果网格

## 10.4 `大底转换`

结构和容错分析类似，但多一个大底切换按钮行。

空大底时：

- 头部文字：`<大底名> 暂无可用号码`
- 元信息：`请先写入 <大底名> 的三位号码数据`
- 结果区提示：`当前基底为空`

## 11. 全屏矩阵功能

这是当前项目最复杂的前端交互，必须按下面规则复现。

## 11.1 打开条件

点击 `展示矩阵` 时：

1. 使用 `currentSourceLines`
2. 所有行必须是 5 位数字
3. 行数至少 3

否则提示：

- `请先加载开奖号码数据`
- 或 `至少需要 3 期开奖号码才能开启全屏矩阵`

## 11.2 扩展 5 位为 9 位

每一行做如下扩展：

```js
expanded = line.slice(-2) + line + line.slice(0, 2)
```

例如：

- 原行 `12345`
- 扩展后 `451234512`

### 目的

让首尾位置在矩阵里形成环状关系，便于斜向和横向检索。

## 11.3 矩阵基础 UI

顶部有：

1. 位次按钮 `第 1 位` 到 `第 5 位`
2. 搜索输入框
3. 搜索按钮
4. 清除搜索按钮
5. 期数下拉按钮
6. `显示命中组合` / `隐藏命中组合` 按钮
7. 单元格大小按钮
8. `退出` 按钮

### 注意

- `显示命中组合` 按钮实际上控制的是右侧“下一位统计”侧栏
- 当前实现中位次选择是单选，不是多选

## 11.4 矩阵表格渲染

### 列

- 左侧固定一列 `期数`
- 右边 9 列编号 `1..9`

### 行

- 每行对应一期开奖结果
- 行号从 `001` 开始显示

### 自适应分页

矩阵不是一个无限高表格，而是根据当前可视高度，把全部行切成多个 table 横向排开。

#### 第一步：估算

```js
usableHeight = max(220, matrixTableWrap.clientHeight)
unitHeight = max(12, cellSize + 1)
rows = floor(usableHeight / unitHeight) - 1
```

#### 第二步：首次渲染后校准

通过真实渲染出来的表头高度和数据行高度，再重算一遍每页行数。

这一步要保留，否则移动端表格高度会不准。

## 11.5 位次 -> 三组 triplet 规则

矩阵命中分析基于最近 3 期原始号码。

设：

- `r1 = 倒数第 3 行`
- `r2 = 倒数第 2 行`
- `r3 = 倒数第 1 行`
- `posIdx` 是 0-based 位次
- `mod5(i) = ((i % 5) + 5) % 5`

则三组 triplet 必须是：

```js
group1 = r1[mod5(posIdx + 2)] + r2[mod5(posIdx + 3)] + r3[mod5(posIdx + 4)]
group2 = r1[posIdx] + r2[posIdx] + r3[posIdx]
group3 = r1[mod5(posIdx + 3)] + r2[mod5(posIdx + 2)] + r3[mod5(posIdx + 1)]
```

这三组在 UI 上显示为：

- `P{pos}-C1: xxx`
- `P{pos}-C2: xxx`
- `P{pos}-C3: xxx`

## 11.6 组合命中扫描

### 搜索方向

必须扫描 8 个方向：

- 横向→
- 横向←
- 纵向↓
- 纵向↑
- 斜向↘
- 斜向↖
- 斜向↙
- 斜向↗

### 命中规则

对矩阵每个起点，取长度 3 的连续路径：

1. 拼成字符串
2. 如果等于 triplet 集合中的某一项，则记录一次命中

记录结构需要包含：

- `sequence`
- `group`
- `direction`
- `positions`
- `nextDigit`
- `nextPosition`

其中 `nextDigit` / `nextPosition` 是在当前方向上再走一步的位置。

## 11.7 Trace 追踪规则

每个 group 除了 combo 按钮，还有 trace 按钮：

- `追踪 ↘ xx`
- `追踪 ↓ xx`
- `追踪 ↙ xx`

这里的 `xx` 是该组 triplet 的后两位。

### `getTraceConfig(posIdx, groupNo)`

#### group 1

- 方向 `↘`
- `upperCol = posIdx`
- `lowerCol = posIdx + 1`
- `nextCol = posIdx + 2`
- 行步长 `dr = 1`
- 列步长 `dc = 1`

#### group 2

- 方向 `↓`
- `upperCol = posIdx + 2`
- `lowerCol = posIdx + 2`
- `nextCol = posIdx + 2`
- `dr = 1`
- `dc = 0`

#### group 3

- 方向 `↙`
- `upperCol = posIdx + 4`
- `lowerCol = posIdx + 3`
- `nextCol = posIdx + 2`
- `dr = 1`
- `dc = -1`

### `findTraceNextPositions`

扫描所有行：

1. 第一行匹配 pair 第一个字符
2. 下一行匹配 pair 第二个字符
3. 若命中，则把第 3 个位置也作为“下一位命中”记录出来

### `findTraceSeedPositions`

检查底部最后两行是否形成 pair。

如果形成，则只高亮这两格，作为“种子追踪”。

## 11.8 下一位统计侧栏

侧栏标题是：

- `下一位统计`

统计分两块：

1. `命中统计`
2. `追踪统计`

每块内部都按 group 汇总下一位数字 `0..9` 的出现次数，显示两行 heatmap：

1. 上面一行显示 `0..9`
2. 下面一行显示对应次数

次数越大，背景红色越深。

### 去重规则

当前实现为了避免同一路径重复计数，使用了归一化去重 key。

必须保留这件事，否则统计会偏大。

#### 组合命中去重 key

路径列索引需要从 9 列映射回 5 位基础列：

```js
toBaseColIndex(expandedCol) = ((expandedCol - 2) % 5 + 5) % 5
```

再用：

```js
${pos}|${direction}|${normalizedPath}|${normalizedNext}
```

#### 追踪去重 key

```js
${pos}|${groupNo}|${normalizedPath}
```

## 11.9 搜索功能

### 输入限制

- 只允许数字
- 最少 3 位
- 最多 24 位

### 搜索行为

在 8 个方向上扫描完全等于查询串的路径。

命中的所有单元格加黄色高亮。

## 11.10 矩阵高亮颜色

### 组合命中颜色

按 group 着色：

- group1：橙色
- group2：蓝色
- group3：绿色
- 多 group 重叠：紫色

### 追踪命中颜色

- 红色

### 搜索命中颜色

- 黄色

### 当前实现说明

虽然 CSS 里保留了 `matrix-hit-pos1..5` 等类，但实际绘制逻辑主要按 group 上色。

复现时不需要复制无用逻辑，但最终视觉结果要一致。

## 12. 样式方向

## 12.1 整体风格

必须保留当前项目的视觉方向：

- 明亮浅蓝系玻璃拟态
- 大面积圆角
- 阴影柔和
- 背景使用多层 radial + linear gradient
- 非暗色主题

### 样式基调变量

```css
--bg: #e8f1ff;
--surface: rgba(255, 255, 255, 0.76);
--surface-strong: rgba(255, 255, 255, 0.9);
--text: #122340;
--text-secondary: #4e668a;
--border: rgba(107, 142, 193, 0.28);
--accent: #2f72ff;
--accent-hover: #1f5de2;
--radius: 18px;
```

### 字体

CSS 里声明了：

- `Outfit`
- `JetBrains Mono`

但当前模板没有真正把字体样式表加载完整。

复现时可以：

1. 正确引入字体
2. 或保留同等视觉效果的替代字体

但不要再留下空的 `<link rel="stylesheet">`。

## 12.2 响应式

需要至少覆盖下面 3 个断点：

- `max-width: 1024px`
- `max-width: 768px`
- `max-width: 480px`

主要变化：

1. 主布局仍保持左右结构，但比例收缩
2. 顶部期和按钮网格列数减少
3. 矩阵顶部控制区改成纵向堆叠
4. 矩阵按钮区域允许横向滚动
5. 结果区域按钮在小屏下改成全宽

## 12.3 动效

保留简短进入动效：

```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); filter: blur(1px); }
  to   { opacity: 1; transform: translateY(0);    filter: blur(0); }
}
```

并支持：

```css
@media (prefers-reduced-motion: reduce)
```

关闭动画。

## 13. 不能照抄的已知问题

下面这些是当前项目的已知问题，复现时必须修掉：

1. `analyze` 对非 5 位输入会有潜在 500 风险。
2. 大底名称如果直接拼接 `innerHTML`，会有存储型 XSS 风险。
3. `get_history` 的 `limit` 目前容易被负数或超大值绕过。
4. `DB_PATH` 不能依赖当前工作目录。
5. 初始化抓取不能阻塞应用启动。
6. 模板中不要保留无意义的空 `<link rel="stylesheet">`。
7. 不要复制当前 CSS/JS 中未使用的历史残留类名或逻辑，除非它们对最终功能有用。

## 14. 推荐实现边界

另一个 AI 在实现时应遵守这些边界：

1. 前端仍然使用原生 JS，不要引入 React/Vue。
2. 后端仍然使用 Flask + SQLite。
3. 接口路径和返回字段名尽量保持一致。
4. 页面文案尽量保持一致。
5. 保留“矩阵始终基于原始 5 位历史数据”的行为。
6. 保留 `period_sum` 只影响主分析区、不影响矩阵的现有逻辑。
7. 大底相关输出始终是三位号。

## 15. 验收清单

另一个 AI 完成后，至少要通过下面这些验收：

### 15.1 启动与初始化

- 应用在没有网络时也能启动
- 首次启动能创建两张表
- 大底库 4 个槽位存在
- `slot=1..4` 的 `sourceCount` 分别是 `798 / 875 / 875 / 720`

### 15.2 历史数据

- `/api/get_history?limit=300` 返回成功
- 返回的数据顺序是旧 -> 新
- `latest_time` 是最新一期时间
- `limit=-1` 不会返回整库

### 15.3 主分析区

- 顶部 20 个按钮可切换
- `1 期` 时左侧显示原始号码
- `k 期和` 时左侧显示逐位和结果
- `遗漏统计` 显示 5 个位置
- `大底生成` 能显示 3 组规则和最终号码
- `大底容错分析` 默认按 `0..2` 出结果
- `大底转换` 能切换 4 个大底

### 15.4 矩阵

- 至少 3 行数据时可以打开
- 打开后显示 9 列环形扩展矩阵
- 可切换位次
- 可切换组合 group 开关
- 可切换 trace 开关
- 可搜索数字串
- 可调期数
- 可调单元格大小
- 可显示/隐藏右侧“下一位统计”

### 15.5 安全

- 大底名称显示时不会执行 HTML / JS
- 所有错误输入都返回明确错误提示，不会 500

## 16. 建议的文件职责拆分

### `app.py`

负责：

- Flask app 初始化
- DB 初始化
- 抓取历史开奖
- 4 个 API
- 所有统计算法

### `templates/index.html`

负责：

- 页面骨架
- 主分析区域
- 矩阵 overlay 结构

### `static/script.js`

负责：

- 页面初始化
- 历史数据加载
- 请求分析接口
- tab 渲染
- 大底复制/下载
- 矩阵构建与交互

### `static/styles.css`

负责：

- 整体视觉风格
- 主页面布局
- tab 和结果卡片样式
- 矩阵 overlay 与响应式

## 附录 A：slot=1 `798大底` 精确种子

将下面内容作为 `dadi_base_sets.slot=1` 的 `numbers_text` 精确写入。

名称固定为：

```text
798大底
```

内容如下：

```text
001
003
005
007
009
010
011
012
013
014
015
016
017
018
019
021
023
025
026
027
028
029
030
031
032
033
034
035
036
037
038
039
041
043
045
047
049
050
051
052
053
054
055
056
057
058
059
061
062
063
065
067
069
070
071
072
073
074
075
076
077
078
079
081
082
083
085
087
089
090
091
092
093
094
095
096
097
098
099
100
101
102
103
104
105
106
107
108
109
110
114
116
118
119
120
124
125
126
127
128
129
130
134
136
138
139
140
141
142
143
144
145
146
147
148
149
150
152
154
156
158
159
160
161
162
163
164
165
166
167
168
169
170
172
174
176
178
179
180
181
182
183
184
185
186
187
188
189
190
191
192
193
194
195
196
197
198
199
201
203
205
206
207
208
209
210
214
215
216
217
218
219
225
226
227
228
229
230
234
235
236
237
238
239
241
243
245
246
247
248
249
250
251
252
253
254
255
256
257
258
259
260
261
262
263
264
265
266
267
268
269
270
271
272
273
274
275
276
277
278
279
280
281
282
283
284
285
286
287
288
289
290
291
292
293
294
295
296
297
298
299
300
301
302
303
304
305
306
307
308
309
310
314
316
318
319
320
324
325
326
327
328
329
330
334
336
338
339
340
341
342
343
344
345
346
347
348
349
350
352
354
356
358
359
360
361
362
363
364
365
366
367
368
369
370
372
374
376
378
379
380
381
382
383
384
385
386
387
388
389
390
391
392
393
394
395
396
397
398
399
401
403
405
407
409
410
411
412
413
414
415
416
417
418
419
421
423
425
426
427
428
429
430
431
432
433
434
435
436
437
438
439
441
443
445
447
449
450
451
452
453
454
455
456
457
458
459
461
462
463
465
467
469
470
471
472
473
474
475
476
477
478
479
481
482
483
485
487
489
490
491
492
493
494
495
496
497
498
499
500
501
502
503
504
505
506
507
508
509
510
512
514
516
518
519
520
521
522
523
524
525
526
527
528
529
530
532
534
536
538
539
540
541
542
543
544
545
546
547
548
549
550
552
554
556
558
560
561
562
563
564
565
566
567
568
569
570
572
574
576
578
580
581
582
583
584
585
586
587
588
589
590
591
592
593
594
596
598
601
602
603
605
607
609
610
611
612
613
614
615
616
617
618
619
620
621
622
623
624
625
626
627
628
629
630
631
632
633
634
635
636
637
638
639
641
642
643
645
647
649
650
651
652
653
654
655
656
657
658
659
661
662
663
665
667
670
671
672
673
674
675
676
677
678
679
681
682
683
685
687
690
691
692
693
694
695
697
700
701
702
703
704
705
706
707
708
709
710
712
714
716
718
719
720
721
722
723
724
725
726
727
728
729
730
732
734
736
738
739
740
741
742
743
744
745
746
747
748
749
750
752
754
756
758
760
761
762
763
764
765
766
767
768
769
770
772
774
776
778
780
781
782
783
784
785
786
787
788
789
790
791
792
793
794
796
798
801
802
803
805
807
809
810
811
812
813
814
815
816
817
818
819
820
821
822
823
824
825
826
827
828
829
830
831
832
833
834
835
836
837
838
839
841
842
843
845
847
849
850
851
852
853
854
855
856
857
858
859
861
862
863
865
867
870
871
872
873
874
875
876
877
878
879
881
882
883
885
887
890
891
892
893
894
895
897
900
901
902
903
904
905
906
907
908
909
910
911
912
913
914
915
916
917
918
919
920
921
922
923
924
925
926
927
928
929
930
931
932
933
934
935
936
937
938
939
940
941
942
943
944
945
946
947
948
949
950
951
952
953
954
956
958
960
961
962
963
964
965
967
970
971
972
973
974
976
978
980
981
982
983
984
985
987
990
991
992
993
994
```
